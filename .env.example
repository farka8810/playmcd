# Copy to .env and adjust.
# PostgreSQL connection string used by db/index.js (node-postgres Pool).
DATABASE_URL=postgres://postgres:postgres@localhost:5432/playmcd

# Port the custom server (Next + Socket.IO) listens on.
PORT=3000
HOST=localhost
